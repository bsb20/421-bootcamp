{
  "version": "0.2.0",
  "configurations": [
    {
      // Backup debug path (independent of the CMake Tools "Debug" button).
      // Debugs whatever target is selected as the CMake "launch target" in the
      // status bar, building it first via ${command:cmake.launchTargetPath}.
      "name": "(gdb) Debug CMake target",
      "type": "cppdbg",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}/build",
      "externalConsole": false,
      "MIMode": "gdb",
      "miDebuggerPath": "/usr/bin/gdb",
      "setupCommands": [
        {
          "description": "Enable pretty-printing for gdb",
          "text": "-enable-pretty-printing",
          "ignoreFailures": true
        }
      ]
    }
  ]
}
